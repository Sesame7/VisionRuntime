runtime:
  save_dir: "data/test"
  detect_queue_capacity: 10

camera:
  type: "mock"
  common:
    save_images: false
    capture_output_format: "bgr8"
  mock:
    image_dir: "data/test"
    order: "name_desc"
    end_mode: "stop"

trigger:
  debounce_ms: 0

detect:
  impl: "overexposure"
  recipe_dir: "../overexposure"
  default_recipe: "default.yaml"
  preview_enabled: false
  preview_max_edge: 1280
  switch_guard_ms: 2000

output:
  hmi:
    enabled: false
    history_size: 5
  modbus:
    enabled: false
  write_csv: false
